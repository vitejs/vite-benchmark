import { Measure } from "./meatures"

export function runCases({ viteDir }: { viteDir: string }) {
  // const measure = new Measure({
  //   func: () => {
  //     console.log("hello")
  //   },
  // })
  // measure.start()
  // measure.end()
  // measure.report()
}
